# Deploy Assets

Deployment assets in this repo are scoped by product/workflow area. A folder
under `deploy/` should not be read as support for airgapping or deploying the
entire repository.

Current scoped deploy targets:

- `nemotron-customizer/airgap/` - airgap packaging for Nemotron Customizer
  workflows represented by `src/nemotron/steps/`.
