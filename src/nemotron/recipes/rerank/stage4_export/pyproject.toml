[project]
name = "recipe-runner-export"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "onnx>=1.16.0,<1.20",
    "onnxruntime>=1.18.0",
    "optimum>=1.20.0",
    "nemo-export-deploy>=0.3.1",
    "onnxscript>=0.5.0",
    # Needed by Nemotron library (added via --with flag)
    "omegaconf>=2.3.0",
    "pydantic-settings>=2.0.0",
]

[project.optional-dependencies]
tensorrt = [
    "tensorrt>=10.13,<10.15",
]

[tool.uv]
override-dependencies = ["transformers>=5,<6"]
# Exclude torch/torchvision from resolution — supplied via --with at runtime
# with UV_TORCH_BACKEND=auto so the correct CUDA variant is resolved.
exclude-dependencies = ["torch", "torchvision", "transformer-engine", "nvidia-pytriton"]
environments = ["python_version >= '3.12' and python_version < '3.14'"]

[tool.nemotron]
entry-point = "export.py"
container-exclude-dependencies = [
    "torch",
    "torchvision",
    "flash-attn",
    "triton",
    "pyarrow",
    "scipy",
    "opencv-python-headless",
    "tensorrt",
    "transformer-engine",
]
