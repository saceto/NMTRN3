positive:
  - create a custom MCQ benchmark from my domain files
  - run BYOB generation for the maths target subject
  - translate a generated BYOB benchmark and preserve answer indexes
  - add a GSM8K benchmark family under the BYOB runtime

negative:
  - translate a general chat training JSONL corpus
  - fine tune a checkpoint with LoRA
  - convert a Megatron checkpoint to Hugging Face
  - evaluate a model on an existing public benchmark only
